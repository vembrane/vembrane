function: "filter"
expression: 'INFO["FLAG"] and 
  INFO["INT"] > 0 and 
  INFO["FLOAT"] == 0.5 and 
  INFO["STRING"] == "String" and  
  INFO["INT2"] == (1, 2) and
  INFO["FLOAT2"] == (0.5, 1.0) and
  INFO["STRING2"] == ("String1", "String2") and
  INFO["INTA"] == 1 and
  INFO["FLOATA"] == 0.5 and
  INFO["STRINGA"] == "StringA" and
  INFO["INTR"] == (1, 2) and
  INFO["FLOATR"] == (0.5, 1.0) and
  INFO["STRINGR"] == ("StringR1", "StringR2") and
  INFO["INTX"] == (1, 2, 3, 4) and
  INFO["FLOATX"] == (0.5, 0.75) and
  INFO["STRINGX"] == ("String1", "String2", "String3") and
  FORMAT["INT"]["Sample"] == 1 and
  FORMAT["INT2"]["Sample"] == (1, 2) and
  FORMAT["INTA"]["Sample"] == 1 and
  FORMAT["INTR"]["Sample"] == (1, 2) and
  FORMAT["INTG"]["Sample"] == (1, 2, 3) and
  FORMAT["INTX"]["Sample"] == (1, 2, 3, 4) and
  FORMAT["FLOAT"]["Sample"] == 0.5 and
  FORMAT["FLOAT2"]["Sample"] == (0.5, 1.0) and
  FORMAT["FLOATA"]["Sample"] == 0.5 and
  FORMAT["FLOATR"]["Sample"] == (0.5, 1.0) and
  FORMAT["FLOATG"]["Sample"] == (0.5, 1.0, 1.5) and
  FORMAT["FLOATX"]["Sample"] == (0.5, 0.75) and
  FORMAT["STRING"]["Sample"] == "String1" and
  FORMAT["STRING2"]["Sample"] == ("String1", "String2") and
  FORMAT["STRINGA"]["Sample"] == "String1" and
  FORMAT["STRINGR"]["Sample"] == ("StringR1", "StringR2") and
  FORMAT["STRINGG"]["Sample"] == ("String1", "String2", "String3") and
  FORMAT["STRINGX"]["Sample"] == ("String1", "String2", "String3")
'
